# 移动端动态模块化与云端协同架构设计

根据你的最新决策，我们确立了核心架构方针：**App 手机端 + 夫妻双人绑定 + 云端同步 + 阶段模块化按需动态下载**。

---

## 一、 “按阶段动态下载” 架构设计 (Dynamic Modular Engine)

### 1. 痛点与架构目标
- **痛点**：跨越24年的应用若将所有功能打包进一个安装包，会导致体积膨胀（100MB+），且备孕用户看到高考、大学记账功能会感到杂乱无章。
- **目标**：
  - **超轻量宿主底座（Shell App，<15MB）**：仅包含用户认证、双人组队、云端同步总线、本地数据库和模块下载挂载引擎。
  - **生命周期功能包（Lifecycle Feature Packs）**：以独立插件包（如 `bundle-preconception.pkg`）形式按需下载，每个包仅几兆，解压即用。
  - **生命周期归档与释放**：孩子出生后，用户可选择将“备孕模块”归档卸载（释放手机存储空间，但所有历史记录和账单永久保留在宿主数据库中）。

### 2. 动态模块化运作全流程

```mermaid
sequenceDiagram
    autonumber
    actor User as 用户（准爸/准妈）
    participant Host as 宿主底座 (Host App)
    participant ModuleMgr as 模块管理器 (ModuleManager)
    participant CDN as 静态资源 CDN / OSS
    participant DB as 本地 SQLite 统一数据库

    User->>Host: 启动并设置状态为“备孕期”
    Host->>ModuleMgr: 查询本地是否已安装【备孕模块】
    alt 未安装
        ModuleMgr->>CDN: 检查包版本并下载 bundle-preconception.pkg
        CDN-->>ModuleMgr: 返回动态代码包与资源
        ModuleMgr->>ModuleMgr: 校验 Hash 完整性并解压到沙箱
        ModuleMgr->>Host: 动态挂载路由与组件
    end
    Host->>DB: 运行该模块专有的建表/升级脚本 (DDL)
    Host-->>User: 渲染专属【备孕期】极简界面
```

### 3. 宿主与模块的代码分层隔离
1. **宿主内核层（固定内置）**：
   - **身份与组队**：JWT鉴权、家庭ID生成、二维码/邀请码绑定。
   - **公共同步引擎**：增量数据云端上传/下发（基于时间戳 + Version 向量）。
   - **本地总线数据仓（SQLite）**：用户表、家庭表、消费账本主表、定时任务调度表。
   - **原生推送通道**：系统日历注入、极光/个推/APNs 原生后台定时提醒。
2. **动态插件层（按需加载）**：
   - `pkg-preconception`（备孕包）：排卵日历算法、基础体温曲线图表、精液与生理指标录入、男性戒断打卡。
   - `pkg-pregnancy`（孕期包）：40周产检全流程甘特图、产检化验单相册、胎动计数器、待产包清单。
   - `pkg-infant-0-1`（0-1岁婴儿包）：喂奶倒计时、大便性状图谱、国家免疫疫苗接种日历。

---

## 二、 夫妻双人绑定与跨端提醒机制 (Family Sync & Reminders)

### 1. 组队与配对协议
- **邀请流程**：一方注册后生成带时效性的 **6位数字密钥** 或 **动态二维码**；另一方扫描/输入后加入同一个 `family_id`。
- **角色标识**：`role: 'mom' | 'dad'`。
- **共享与隐私边界**：
  - **默认完全共享**：产检安排、叶酸打卡状态、公共养育账单。
  - **私密保护开关**：支持某些日记、私密病历仅对自己可见。

### 2. 双人联动提醒场景
- **定向促办提醒**：
  - 准妈妈设定规则：“每天 21:00 提醒准爸爸戒烟/补充番茄红素”。
  - 准爸爸手机接收到本地推送。
- **打卡状态互知**：
  - 准妈妈在手机上勾选“今日已服叶酸”，准爸爸端界面实时变为“已完成（10分钟前已服）”，避免重复催促。
- **重大日程双人强提醒**：
  - 产检前1天与当天早晨，系统同时向双方设备推送系统通知，并支持一键写入手机原生系统日历。

---

## 三、 MVP阶段 核心数据库模型设计 (Database Schema)

底层采用 **“统一大账本 + 阶段专用业务表”** 的设计理念，确保未来扩展24年其他阶段时，核心财务与日程引擎完全复用。

```mermaid
erDiagram
    FAMILIES ||--o{ USERS : "包含"
    FAMILIES ||--o{ EXPENSES : "共享记账"
    FAMILIES ||--o{ REMINDERS : "共享提醒任务"
    FAMILIES ||--o{ PRECONCEPTION_LOGS : "备孕记录"
    FAMILIES ||--o{ PREGNANCY_CHECKUPS : "产检记录"

    FAMILIES {
        string family_id PK "家庭唯一标识"
        string current_stage "当前阶段: preconception / pregnancy"
        date target_date "末次月经或预产期"
        datetime created_at "创建时间"
    }

    USERS {
        string user_id PK "用户ID"
        string family_id FK "关联家庭"
        string role "角色: mom / dad"
        string nickname "昵称"
        string push_token "设备推送标识"
    }

    REMINDERS {
        string reminder_id PK "提醒ID"
        string family_id FK "家庭ID"
        string target_role "接收人: mom / dad / both"
        string title "如: 吃叶酸 / 排卵试纸打卡"
        string cron_rule "周期规则: 每天 08:30"
        boolean is_active "是否启用"
    }

    EXPENSES {
        string expense_id PK "记账ID"
        string family_id FK "家庭ID"
        string creator_id FK "记账人"
        decimal amount "金额"
        string stage "所处阶段: preconception/pregnancy"
        string category "分类: 检查费/补剂营养/大件用品"
        date happen_date "消费日期"
        string remark "备注"
    }

    PRECONCEPTION_LOGS {
        string log_id PK "记录ID"
        string family_id FK "家庭ID"
        date record_date "记录日期"
        float basal_body_temp "女方基础体温(℃)"
        string ovulation_result "排卵试纸状态: 阴/弱阳/强阳"
        boolean dad_smoke_free "男方今日是否无烟"
        boolean dad_alcohol_free "男方今日是否无酒"
        json extra_metrics "其他体征扩展字段"
    }

    PREGNANCY_CHECKUPS {
        string checkup_id PK "产检ID"
        string family_id FK "家庭ID"
        int gestational_week "孕周(如: 12)"
        string checkup_name "检查名称(如: NT检查)"
        date planned_date "计划检查日期"
        date actual_date "实际检查日期"
        string status "状态: pending / completed"
        string doctor_advice "医生指导意见"
        json attachment_urls "化验单图片路径列表"
    }
```

---

## 四、 移动端技术选型推荐

要实现 **跨 iOS/Android + 动态按需加载模块 + 强后台推送**，推荐以下两种技术方案：

| 方案 | 架构组合 | 模块动态下载实现方式 | 优势 | 推荐度 |
| :--- | :--- | :--- | :--- | :--- |
| **方案 A (推荐)** | **Uni-app (Vue 3 + TS) + 原生离线包/分包引擎** | 1. 线上主包极小<br>2. 各阶段作为独立子包/WGT热更资源包，由宿主动态从云端下载并解压装载。 | 1. 国内生态完善，原生推送、微信生态支持成熟。<br>2. 学习曲线平缓，全端支持。<br>3. 极其容易实现热重载与模块下发。 | ★★★★★ |
| **方案 B** | **React Native + Re.Pack (Module Federation)** | 利用 Webpack Module Federation 将备孕、产检拆分成独立的 Remote JS Chunks 异步载入。 | 1. 原生性能与手感好。<br>2. 现代化微前端架构。 | ★★★★☆ |
| **方案 C** | **Flutter + Hybrid Web/Engine** | 核心壳原生 Flutter，各阶段业务采用离线高保真 WebContainer/Flutter Dynamic 模块加载。 | 1. 极高颜值的图表和动画。<br>2. 动态下发在 iOS 端受到苹果审核策略限制较严格。 | ★★★☆☆ |

---

## 五、 后续开发推进路线

1. **Step 1（接口与数据库定义）**：编写 RESTful / WebSocket API 接口契约，确定双人同步机制。
2. **Step 2（宿主框架搭建）**：初始化客户端工程，实现家庭创建、邀请码配对和主壳框架。
3. **Step 3（开发 Module 1：备孕模块）**：实现双方健康打卡、定时吃药提醒、备孕专属记账。
4. **Step 4（开发 Module 2：孕期模块）**：实现产检全周期看板、报告云相册、孕期记账。
